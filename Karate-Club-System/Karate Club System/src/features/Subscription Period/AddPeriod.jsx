import StyledNavLinke from "../../ui/StyledNavLinke";

function AddPeriod() {
  return (
    <StyledNavLinke to={`/add-subscription-Pireod/${0}`}>
      <span>Add new subscription period</span>
    </StyledNavLinke>
  );
}

export default AddPeriod;
