import StyledNavLinke from "../../ui/StyledNavLinke";

function AddTest() {
  return (
    <StyledNavLinke to={`/take-next-belt-test`}>
      <span>Take Next Belt Test</span>
    </StyledNavLinke>
  );
}

export default AddTest;
